Task 15 doc example path verification

Checked against src/lib.rs exports:
- Root re-export: SpecValidator, ValidationIssue
- Module export: section::Entry (alias of section::LogEntry)
- Module export: section::StandardItems::*

Checks performed:
1) Searched docs/plan/API.md for stale old crate import paths `use vhdx::...` and `vhdx::section::...`.
2) Verified key export lines in src/lib.rs for `SpecValidator`, `section::Entry`, and `section::StandardItems`.
3) Confirmed validator entry style remains `file.validator()` with `SpecValidator<'_>` / `validation::SpecValidator<'_>` equivalence in docs.
4) Confirmed README.md does not exist at repo root, so no README update was needed.

Corrections made:
- docs/plan/API.md:
  - Replaced example imports from `vhdx::...` to `vhdx_rs::...` in usage examples.
  - Updated top API tree crate label from `vhdx::` to `vhdx_rs::`.
  - Added explicit note that `section::Entry` is a compatibility alias of `section::LogEntry`.

Why corrected:
- Current package name and import path in this repository is `vhdx-rs` (code path `vhdx_rs`), so `vhdx::...` examples are stale and not directly usable.
- Added alias note to reduce ambiguity between Entry and LogEntry while keeping current re-export contract.

Verification results:
- stale import count in docs/plan/API.md: 0
- cargo test --workspace: PASS
- markdown LSP diagnostics: not available in this environment; recorded as known limitation and replaced with executable path checks + workspace tests.
