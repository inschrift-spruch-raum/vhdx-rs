## [2026-04-18 任务：T5 BAT 条目迭代器签名对齐]
- API.md 仍记录 `entries(&self) -> BatEntryIter<'_>` 和 `BatEntryIter<'a>` 类型，与新的 `Vec<BatEntry>` 签名不一致。API.md 更新不在本任务范围内（本任务明确修改文件仅 `bat.rs` + `integration_test.rs`）。
- `entry(&self, index: u64)` 的 index 参数类型与 API.md 的 `usize` 不一致，属于后续任务。

## [2026-04-19 任务：T8 Sections/子结构签名与可见性结构对齐]
- 未触碰 `Sections<'a>` 泛型外形（当前为 `Sections` + `RefCell` 持有），避免借用模型放大改动；后续若严格追求 API.md 形态可在独立任务处理。

## [2026-04-19T00:29:16+08:00] 任务：T9 ParentLocator/KeyValueEntry 签名收口
- `FileParameters<'a>`（API.md 有生命周期参数）与代码 `FileParameters`（无生命周期参数、`raw_data` 为拥有所有权的 `[u8; 8]`）仍为已知差异，属于后续任务范围。

## [2026-04-19T01:30:00+08:00] 任务：T10 API 接口冒烟测试
- 已知 API.md 与代码差异（均已在冒烟测试中适配，无需库代码修改）：
  - 多个 section 类型（Header/Bat/Metadata/Log/Sections）无生命周期参数，API.md 声明有。
  - Guid 没有 new_v4()，仅有 from_bytes/nil。
  - RegionTable.header/entries 是方法而非公共字段。
  - crc32c_with_zero_field 接受 3 个独立参数而非 Range。

## [2026-04-19] 任务：最终波次拒绝项修复（证据回填）
- 历史阻断项“任务级证据缺失”已通过文件回填处置，但回填证据中部分内容属于重建摘要而非原始逐行终端输出。
- 风险提示：若后续审计升级为“必须附完整原始命令输出”，当前回填文件需要补充原始日志附件或重新执行并留档。

## [2026-04-19] 任务：F1 计划符合性复审（修复后）
- 阻断项 #1（证据质量）：28 个 task-N-*.txt 全部为“重建摘要”回填文件（每个约 7 行、340-423 字节），非原始终端命令输出。证据文件名契约已满足（全部存在），但内容质量无法独立证明任务在执行时通过了可执行验证。
- 中等级别差异：FileParameters 缺少 `<'a>`、RegionTable 的 header/entries 为方法而非公共字段。
- 证据现状：`task-12-quality-gate.md` 是唯一原始证据（T12 执行时产生）；其余 28 个回填文件均标注“来自已验证运行的重建摘要”并带有指向 notepads 的可追溯链接。
- 机械验收门（cargo test/clippy/fmt/smoke）全部通过（152 测试通过），但核心目标中的“严格对齐”不满足。

## [2026-04-19] 任务：F1 计划符合性复审 #2（范围修复后）
- 28/28 证据文件名契约满足（全部为重建摘要，T12 有原始证据锚点）。
- 已知残留差异（D1-D8）已分类：D1（生命周期形态）为高优先级，但在 T8 执行时已明确延期并记录；D2-D8 为中/低优先级且功能等价。
- 条件：D1 生命周期形态差异作为技术债务追踪；证据质量为重建摘要（非原始终端输出）。

## [2026-04-19] 任务：F1 计划符合性审计（最终波次复审 #3）
- 剩余的非阻断跟踪项：D1 生命周期形态偏差（技术债务）、证据质量（重建摘要 vs 原始日志）。

## [2026-04-19] 任务：F2 代码质量评审
- 已确认既有问题保持不变：动态磁盘读取返回零（`file.rs:229`）、未使用的 IO 方法（`io_module.rs:73-116`）、`BatEntry::new` 死代码（`bat.rs:188`）。
- 次要发现：`bat.rs:48-63` 的 `filter_map` 会静默丢弃无效 BAT 条目（在损坏文件中可能造成数据丢失，但与当前设计理念一致）。

## [2026-04-19T00:14:23.1390984+08:00] 任务：T13 validation 模块与公开校验类型
- `cargo test -p vhdx-rs` 仍有既有的 `dead_code` 警告（`read_sectors/write_sectors`、`BatEntry::new`），与本次变更无关，未新增警告。

## [2026-04-19] 任务：F2 代码质量评审（最终波次新鲜复审）
- 已确认既有问题保持不变：动态磁盘读取返回零（`file.rs:219`）、未使用的 IO 方法（`io_module.rs:73,103`）、`BatEntry::new` 死代码（`bat.rs:188`）、`flush_raw` 死代码（`file.rs:305`）。
- Clippy 警告数量稳定在 108（库 102 + CLI 6），无回归。
